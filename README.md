## SIFT over augmentations
Try different SIFT detectors (OpenCV and scikit-image) to find the matches between initial image and its transformed versions.

Judge Judy's image is taken from dataset CelebA HQ.
